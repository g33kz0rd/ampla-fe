interface ColumnProps {}

export const Column = ({}: ColumnProps) => {
  return (
    <div
      style={{
        width: 150,
        height: 25,
        border: "solid",
        borderWidth: 1,
      }}
    />
  );
};
